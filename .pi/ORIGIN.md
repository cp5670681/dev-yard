Primitives (`grilling`, `domain-modeling`, `tdd`, `codebase-design`) are copies from https://github.com/mattpocock/skills (MIT).

`fetch-requirement`, `grill-with-docs`, `to-spec`, `to-tickets`, `implement`, `code-review` are yard I/O adapters of those skills: artifacts live under `reqs/<JIRA>/`, not GitHub/Linear/`.scratch`.
